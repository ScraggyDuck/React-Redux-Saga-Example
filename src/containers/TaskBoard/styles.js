const styles = theme => ({
  box: {
    display: 'flex',
    alignItems: 'center'
  },
  shape: {
    margin: '10px',
    padding: '10px',
    backgroundColor: theme.color.secondary
  }
});

export default styles;
