import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  color: {
    primary: '#D32F2F',
    secondary: '#4CAF50',
    error: '#E64A19'
  },
  typography: {
    textColor: '#FFFFFF'
  }
});

export default theme;
